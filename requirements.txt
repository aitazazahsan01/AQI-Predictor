# Runtime dependencies: pipelines, dashboard and tests.
#
# Deliberately excludes TensorFlow. It is only needed to *train* the LSTM
# candidate, is ~230MB, and has no wheels for the newest Python releases -
# which breaks deployment targets that default to a very recent interpreter.
# Training installs requirements-train.txt instead.

# Core pipeline
requests
python-dotenv
pandas
hopsworks[python]

# Modelling (Module 4) - also needed at serving time to unpickle models
scikit-learn
xgboost
statsmodels
joblib

# Dashboard and explainability (Modules 6 and 7)
streamlit
shap

# Testing
pytest
pyyaml
