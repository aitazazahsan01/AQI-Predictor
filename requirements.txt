# Core pipeline
requests
python-dotenv
pandas
hopsworks[python]

# Modelling (Module 4)
scikit-learn
xgboost
statsmodels
joblib

# Deep learning candidate (Module 4). Heavy, and the LSTM is skipped
# automatically when it is absent, so it can be dropped on constrained
# environments without breaking the training run.
tensorflow-cpu>=2.15

# Dashboard and explainability (Modules 6 and 7)
streamlit
shap

# Testing
pytest
pyyaml
